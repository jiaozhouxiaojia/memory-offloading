# 配置示例
CGROUP = /sys/fs/cgroup/test
PSI_THRESHOLD = 0.1    # PSI 阈值，微秒
RECLAIM_RATIO = 0.1        # 回收比例，0 到 1 之间
MIN_SIZE = 26214400      # 最小内存限制，字节（25M）
MAX_SIZE = 4294967296      # 最大内存限制，字节（4GB）
INTERVAL = 5              # 采样间隔，秒
PRESSURE = 1000000         # 目标压力，微秒
